{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile with configuration for the desktop Mac application",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The unique user ID" },
        "email": { "type": "string", "format": "email", "description": "User email address" },
        "displayName": { "type": "string", "description": "User's display name" },
        "photoURL": { "type": "string", "description": "User profile photo URL" },
        "apiToken": { "type": "string", "description": "Secret access token for macOS Mac client auth" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Profile creation timestamp" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Profile update timestamp" }
      },
      "required": ["userId", "email", "createdAt", "apiToken"]
    },
    "Backup": {
      "title": "Backup",
      "description": "Information about a message backup uploaded from the desktop macOS client app",
      "type": "object",
      "properties": {
        "backupId": { "type": "string", "description": "Unique ID of the backup archive" },
        "userId": { "type": "string", "description": "User UID who owns this backup" },
        "deviceName": { "type": "string", "description": "Friendly name of the macOS device" },
        "appVersion": { "type": "string", "description": "Version of the Message Backup macOS desktop app" },
        "messageCount": { "type": "integer", "description": "Total number of messages backed up" },
        "chatCount": { "type": "integer", "description": "Total number of active chat conversations" },
        "sizeBytes": { "type": "integer", "description": "Compressed backup payload size in bytes" },
        "status": { "type": "string", "enum": ["uploading", "completed", "failed"], "description": "Status of the backup" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Creation timestamp in Firestore" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Last update timestamp" }
      },
      "required": ["backupId", "userId", "deviceName", "status", "createdAt"]
    },
    "Chat": {
      "title": "Chat",
      "description": "A conversation/chat contact item in a backup",
      "type": "object",
      "properties": {
        "chatId": { "type": "string", "description": "Unique chat room/conversation ID" },
        "userId": { "type": "string", "description": "User who owns this chat thread" },
        "backupId": { "type": "string", "description": "Backup ID this chat belongs to" },
        "service": { "type": "string", "enum": ["iMessage", "SMS", "WhatsApp"], "description": "Chat protocol source" },
        "contactName": { "type": "string", "description": "Readable display name of the contact" },
        "contactIdentifier": { "type": "string", "description": "Email or phone number of the target" },
        "messageCount": { "type": "integer", "description": "Count of messages in this chat" },
        "lastMessageText": { "type": "string", "description": "Body preview of the latest message" },
        "lastMessageAt": { "type": "string", "format": "date-time", "description": "Timestamp of the latest message" }
      },
      "required": ["chatId", "userId", "backupId", "service", "contactName", "contactIdentifier"]
    },
    "Message": {
      "title": "Message",
      "description": "An individual message inside a conversation",
      "type": "object",
      "properties": {
        "messageId": { "type": "string", "description": "Unique message ID inside the backup" },
        "userId": { "type": "string", "description": "User who owns this message" },
        "chatId": { "type": "string", "description": "Chat collection reference" },
        "text": { "type": "string", "description": "Body message text" },
        "isFromMe": { "type": "boolean", "description": "Whether the message was sent by the user" },
        "senderIdentifier": { "type": "string", "description": "Who sent this message (Phone/Email or 'me')" },
        "timestamp": { "type": "string", "format": "date-time", "description": "Message timestamp" }
      },
      "required": ["messageId", "userId", "chatId", "text", "isFromMe", "timestamp"]
    }
  },
  "firestore": {
    "users": {
      "schema": "User",
      "description": "User profiles and desktop credentials"
    },
    "backups": {
      "schema": "Backup",
      "description": "Root backups container queryable across all users safely with rule-based access control"
    },
    "chats": {
      "schema": "Chat",
      "description": "Root chats container queryable safely with rule-based access control"
    },
    "messages": {
      "schema": "Message",
      "description": "Root messages container queryable safely with rule-based access control"
    }
  }
}
